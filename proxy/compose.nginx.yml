# Auto-generated by proxy/nginx/gen_nginx.py — DO NOT EDIT BY HAND.
#
# Compose OVERRIDE that puts an nginx L7 proxy in front of every gRPC backend
# so scaling actually works (per-request DNS round-robin over HTTP/2). Apply with:
#   docker compose -f microservices-demo/docker-compose.yml -f proxy/compose.nginx.yml up -d
# then scale a backend, e.g.:  ... up -d --scale productcatalogservice=4
# Clients keep their code: only their *_SERVICE_ADDR now point at the nginx sidecars.

services:

  # --- nginx sidecars (one per gRPC backend) --------------------------------
  nginx-productcatalogservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/productcatalogservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - productcatalogservice

  nginx-currencyservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/currencyservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - currencyservice

  nginx-cartservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/cartservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - cartservice

  nginx-recommendationservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/recommendationservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - recommendationservice

  nginx-shippingservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/shippingservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - shippingservice

  nginx-paymentservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/paymentservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - paymentservice

  nginx-emailservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/emailservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - emailservice

  nginx-adservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/adservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - adservice

  nginx-checkoutservice:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ../proxy/nginx/checkoutservice.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - checkoutservice

  # --- Repoint each client's gRPC targets at the nginx sidecars -------------
  frontend:
    environment:
      PRODUCT_CATALOG_SERVICE_ADDR: "nginx-productcatalogservice:3550"
      CURRENCY_SERVICE_ADDR: "nginx-currencyservice:7000"
      CART_SERVICE_ADDR: "nginx-cartservice:7070"
      RECOMMENDATION_SERVICE_ADDR: "nginx-recommendationservice:8080"
      SHIPPING_SERVICE_ADDR: "nginx-shippingservice:50051"
      CHECKOUT_SERVICE_ADDR: "nginx-checkoutservice:5050"
      AD_SERVICE_ADDR: "nginx-adservice:9555"

  checkoutservice:
    environment:
      PRODUCT_CATALOG_SERVICE_ADDR: "nginx-productcatalogservice:3550"
      SHIPPING_SERVICE_ADDR: "nginx-shippingservice:50051"
      PAYMENT_SERVICE_ADDR: "nginx-paymentservice:50051"
      EMAIL_SERVICE_ADDR: "nginx-emailservice:8080"
      CURRENCY_SERVICE_ADDR: "nginx-currencyservice:7000"
      CART_SERVICE_ADDR: "nginx-cartservice:7070"

  recommendationservice:
    environment:
      PRODUCT_CATALOG_SERVICE_ADDR: "nginx-productcatalogservice:3550"
