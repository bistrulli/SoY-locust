# Auto-generated by proxy/envoy/gen_envoy.py — DO NOT EDIT BY HAND.
#
# Compose OVERRIDE that puts an Envoy L7 proxy in front of every gRPC backend
# so scaling actually works (per-request round-robin over HTTP/2). Apply with:
#   docker compose -f microservices-demo/docker-compose.yml -f proxy/compose.envoy.yml up -d
# then scale a backend, e.g.:  ... up -d --scale productcatalogservice=4
# Clients keep their code: only their *_SERVICE_ADDR now point at the Envoys.

services:

  # --- Envoy sidecars (one per gRPC backend) --------------------------------
  envoy-productcatalogservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-productcatalogservice"]
    volumes:
      - ../proxy/envoy/productcatalogservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - productcatalogservice

  envoy-currencyservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-currencyservice"]
    volumes:
      - ../proxy/envoy/currencyservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - currencyservice

  envoy-cartservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-cartservice"]
    volumes:
      - ../proxy/envoy/cartservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - cartservice

  envoy-recommendationservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-recommendationservice"]
    volumes:
      - ../proxy/envoy/recommendationservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - recommendationservice

  envoy-shippingservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-shippingservice"]
    volumes:
      - ../proxy/envoy/shippingservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - shippingservice

  envoy-paymentservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-paymentservice"]
    volumes:
      - ../proxy/envoy/paymentservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - paymentservice

  envoy-emailservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-emailservice"]
    volumes:
      - ../proxy/envoy/emailservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - emailservice

  envoy-adservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-adservice"]
    volumes:
      - ../proxy/envoy/adservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - adservice

  envoy-checkoutservice:
    image: envoyproxy/envoy:v1.31-latest
    restart: unless-stopped
    command: ["-c", "/etc/envoy/envoy.yaml", "--service-cluster", "envoy-checkoutservice"]
    volumes:
      - ../proxy/envoy/checkoutservice.yaml:/etc/envoy/envoy.yaml:ro
    depends_on:
      - checkoutservice

  # --- Repoint each client's gRPC targets at the Envoys ---------------------
  frontend:
    environment:
      PRODUCT_CATALOG_SERVICE_ADDR: "envoy-productcatalogservice:3550"
      CURRENCY_SERVICE_ADDR: "envoy-currencyservice:7000"
      CART_SERVICE_ADDR: "envoy-cartservice:7070"
      RECOMMENDATION_SERVICE_ADDR: "envoy-recommendationservice:8080"
      SHIPPING_SERVICE_ADDR: "envoy-shippingservice:50051"
      CHECKOUT_SERVICE_ADDR: "envoy-checkoutservice:5050"
      AD_SERVICE_ADDR: "envoy-adservice:9555"

  checkoutservice:
    environment:
      PRODUCT_CATALOG_SERVICE_ADDR: "envoy-productcatalogservice:3550"
      SHIPPING_SERVICE_ADDR: "envoy-shippingservice:50051"
      PAYMENT_SERVICE_ADDR: "envoy-paymentservice:50051"
      EMAIL_SERVICE_ADDR: "envoy-emailservice:8080"
      CURRENCY_SERVICE_ADDR: "envoy-currencyservice:7000"
      CART_SERVICE_ADDR: "envoy-cartservice:7070"

  recommendationservice:
    environment:
      PRODUCT_CATALOG_SERVICE_ADDR: "envoy-productcatalogservice:3550"
