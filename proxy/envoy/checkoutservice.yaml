# Auto-generated by proxy/envoy/gen_envoy.py — DO NOT EDIT BY HAND.
# Envoy sidecar for `checkoutservice`: per-request gRPC (HTTP/2) load balancing across all
# replicas of `checkoutservice`. STRICT_DNS resolves EVERY replica IP from Compose DNS, and
# ROUND_ROBIN spreads each request (not each connection) across them.
admin:
  address:
    socket_address: { address: 0.0.0.0, port_value: 9901 }
static_resources:
  listeners:
  - name: ingress
    address:
      socket_address: { address: 0.0.0.0, port_value: 5050 }
    filter_chains:
    - filters:
      - name: envoy.filters.network.http_connection_manager
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
          stat_prefix: grpc_checkoutservice
          codec_type: AUTO
          route_config:
            name: local
            virtual_hosts:
            - name: checkoutservice
              domains: ["*"]
              routes:
              - match: { prefix: "/" }
                route:
                  cluster: checkoutservice
                  timeout: 0s          # do not cap gRPC calls: app p95 can reach tens of seconds under load
          http_filters:
          - name: envoy.filters.http.router
            typed_config:
              "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
  clusters:
  - name: checkoutservice
    connect_timeout: 1s
    type: STRICT_DNS               # resolve ALL replica IPs (LOGICAL_DNS would keep only the first)
    dns_lookup_family: V4_ONLY
    dns_refresh_rate: 5s           # re-resolve to pick up scale up / down
    respect_dns_ttl: true
    lb_policy: ROUND_ROBIN         # <-- the fix: per-request spread across replicas
    typed_extension_protocol_options:
      envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
        "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
        explicit_http_config:
          http2_protocol_options: {}     # gRPC upstream speaks HTTP/2
    load_assignment:
      cluster_name: checkoutservice
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address: { address: checkoutservice, port_value: 5050 }
